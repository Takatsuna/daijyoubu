<%= form_for(@kokyaku) do |f| %>
  <% if @kokyaku.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kokyaku.errors.count, "error") %> prohibited this kokyaku from being saved:</h2>

      <ul>
      <% @kokyaku.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
