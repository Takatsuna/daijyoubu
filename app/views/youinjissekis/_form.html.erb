<%= form_for(@youinjisseki) do |f| %>
  <% if @youinjisseki.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@youinjisseki.errors.count, "error") %> prohibited this youinjisseki from being saved:</h2>

      <ul>
      <% @youinjisseki.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :youinwariate_id %><br>
     <%= f.collection_select :youinwariate_id, Youinwariate.all, :id, :id %>
  </div>
  <div class="field">
    <%= f.label :sagyouhoukoku_id %><br>
    <%= f.collection_select :sagyouhoukoku_id, Sagyouhoukoku.all, :id, :id %>
	
  </div>
  <div class="field">
    <%= f.label :kadouhour %><br>
    <%= f.text_field :kadouhour %>
  </div>
  <div class="field">
    <%= f.label :kadoumin %><br>
    <%= f.text_field :kadoumin %>
  </div>
  <div class="field">
    <%= f.label :nengetsu %><br>
    <%= f.text_field :nengetsu %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
